[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "regimex-api"
version = "0.1.0"
description = "RegimeX Open-Source Market Intelligence Platform — FastAPI Backend"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "TBD — see V02/NONFUNCTIONAL_REQUIREMENTS.md OQ-010" }
keywords = ["market-intelligence", "regime-detection", "quantitative-finance", "fastapi"]

[project.urls]
Repository = "https://github.com/luccy93/RegimeX"

# =============================================================================
# Runtime dependencies
# =============================================================================
[project.dependencies]
# Core web framework
fastapi = ">=0.111.0,<1.0.0"
uvicorn = { version = ">=0.29.0,<1.0.0", extras = ["standard"] }

# Configuration
pydantic = ">=2.7.0,<3.0.0"
pydantic-settings = ">=2.2.0,<3.0.0"

# HTTP utilities
httpx = ">=0.27.0,<1.0.0"

# Async support
anyio = ">=4.3.0,<5.0.0"

# Initial Market Data Provider (V05 Commit 02)
yfinance = ">=0.2.40,<1.0.0"
pandas = ">=2.2.0,<3.0.0"

# Security & Authentication (V17 Commit 01)
argon2-cffi = ">=23.1.0,<26.0.0"
pyjwt = ">=2.8.0,<3.0.0"

# =============================================================================
# Optional dependencies — installed in their respective volumes
# =============================================================================
[project.optional-dependencies]

# V05 — Market Data Engine
database = [
    "sqlalchemy[asyncio]>=2.0.30,<3.0.0",
    "asyncpg>=0.29.0,<1.0.0",
    "alembic>=1.13.0,<2.0.0",
]

# V05 — Redis / Celery
workers = [
    "celery[redis]>=5.4.0,<6.0.0",
    "redis>=5.0.0,<6.0.0",
]

# V07–V10 — Analytics & ML
analytics = [
    "numpy>=1.26.0,<3.0.0",
    "pandas>=2.2.0,<3.0.0",
    "scikit-learn>=1.4.0,<2.0.0",
    "scipy>=1.13.0,<2.0.0",
    "statsmodels>=0.14.0,<1.0.0",
]

# V08 — Regime Detection
ml = [
    "hmmlearn>=0.3.2,<1.0.0",
]

# Observability
observability = [
    "structlog>=24.1.0,<25.0.0",
    "prometheus-client>=0.20.0,<1.0.0",
]

# Development tooling
dev = [
    "pytest>=8.2.0,<9.0.0",
    "pytest-asyncio>=0.23.0,<1.0.0",
    "pytest-cov>=5.0.0,<6.0.0",
    "httpx>=0.27.0,<1.0.0",
    "ruff>=0.4.0,<1.0.0",
    "mypy>=1.10.0,<2.0.0",
]

# =============================================================================
# Tool configuration
# =============================================================================

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = [
    "--strict-markers",
    "--tb=short",
    "-v",
]
markers = [
    "unit: Pure unit tests — no external dependencies",
    "integration: Tests requiring real infrastructure (database, Redis)",
    "e2e: End-to-end tests against a running application",
]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["app", "tests"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # Pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "UP",   # pyupgrade
    "ANN",  # flake8-annotations (ANN0xx, ANN2xx)
    "S",    # flake8-bandit (security)
    "N",    # pep8-naming
]
ignore = [
    # ANN101/ANN102 removed in Ruff 0.6+ — self/cls are implied
    "ANN204", # missing return type for __init__ — implied None
    "S101",   # use of assert — acceptable in tests and domain invariants
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101", "ANN", "S105", "S106"]
"app/main.py" = ["ANN001", "ANN202"]  # middleware call_next typing workaround

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
no_implicit_reexport = true
# Set to false to surface missing stubs rather than silently skipping
ignore_missing_imports = false
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
# Third-party libraries without complete type stubs
module = [
    "fastapi.*",
    "yfinance",
    "yfinance.*",
    "pandas",
    "pandas.*",
    "sklearn.*",
    "hmmlearn.*",
    "scipy.*",
]
ignore_missing_imports = true


[[tool.mypy.overrides]]
# Test files: relax strict return-type requirements
module = ["tests.*"]
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_untyped_decorators = false

[tool.coverage.run]
source = ["app"]
omit = ["tests/*", "**/__init__.py"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = false
